{
  "version": "2.0.0",
  "tasks": [
    {
      "label": "Build server",
      "type": "shell",
      "command": "pnpm run compile",
      "options": {
        "cwd": "${workspaceFolder}/server"
      }
    },
    {
      "label": "Build client",
      "type": "shell",
      "command": "pnpm run compile",
      "options": {
        "cwd": "${workspaceFolder}"
      }
    },
    {
      "label": "Build all",
      "dependsOn": [
        "Build server",
        "Build client"
      ],
      "dependsOrder": "sequence"
    }
  ]
}